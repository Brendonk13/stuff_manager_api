
# import pre_save_action from .receivers.pre_save_action
# from django.core.signals import request_finished
# from django.db.models.signals import pre_save

# one of the main purposes of this will be to handle saving update history so we can trace a projects history

# have a central place for signals so we can see which order they are called in!
# pre_save.connect(pre_save_action)
